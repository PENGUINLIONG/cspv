#version 460

layout(binding=1)
uniform Uniform {
    int x;
} u;

void main() {
    int _0 = 1;
    int _1 = u.x + 1;
    int _2 = 1 + (u.x + 1);
    int _3 = 1 + (1 + u.x);
    int _4 = 1 + (u.x + u.x);
    int _5 = u.x + (1 + u.x);
    int _6 = u.x + u.x + u.x;
    int _7 = u.x + (u.x + u.x);
    int _8 = (1 + u.x) + (2 + u.x);
}
