#version 460

layout(binding=1)
uniform Uniform {
    int x;
} u;

void main() {
    int _0 = 2 * u.x;
    int _1 = u.x + (5 * u.x);
    int _2 = (4 + (u.x + 3)) * 2;
    int _3 = (6 + (3 + u.x)) * u.x;
    int _4 = (2 + (u.x + u.x) * 3);
    int _5 = (u.x * 3) + (1 + u.x);
    int _6 = u.x * 3 + u.x * u.x ;
    int _7 = u.x * (u.x * 3 + u.x);
    int _8 = (3 * u.x) * (2 * u.x);
}
